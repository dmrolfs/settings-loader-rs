# Nix
.direnv/
.envrc.local
result
result-*

# Rust
# will have compiled files and executables
/target/
*.pdb

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

.idea/
*.iml

# History/Planning docs
# Note: Task documentation (history/trtl-*-plan.md, history/trtl-*-design.md) IS tracked
# Ephemeral session notes are NOT tracked
history/
!history/trtl-*.md

# Beads
.beads/beads.db
.beads/beads.db-*
.beads/bd.sock
.beads/bd.pipe
.beads/.exclusive-lock
.git/beads-worktrees

# Global agent context (symlink to personal context repo)
.ai-context
.DS_Store
